# Python 3.10 or 3.11 recommended.
numpy>=1.26
librosa>=0.10.2
soundfile>=0.12
torch==2.5.1
# ExecuTorch export tooling. Match the version to the executorch-android AAR
# coordinate in app/gradle/libs.versions.toml.
executorch==0.5.0
# QNN export additionally needs the Qualcomm AI Engine Direct SDK (QAIRT/QNN)
# installed separately and QNN_SDK_ROOT set — see README.md.
